// variables
export let editando = false;
export function edicion(input) {
    editando = input;
}

// selectores
export const contenedorListado = document.querySelector('#listado-clientes');
export const formulario = document.querySelector('#formulario');
